---
title: DoL Game Version Check
---

import { Badge, Tab, Tabs, PackageManagerTabs, Steps, SourceCode } from "@rspress/core/theme";

# @scml/mod-check-game-version

DoL 游戏版本检测的 TypeScript 类型定义。

## 安装

<PackageManagerTabs command="install -D @scml/mod-check-game-version" />

## 快速开始

<Steps>

### 添加类型引用

```ts
/// <reference types="@scml/mod-check-game-version" />
```

### 使用全局实例

`modCheckGameVersion` 已挂载到 `window` 上。

</Steps>

## 全局类型 <Badge type="info" text="global" />

| 属性                  | 类型               | 说明              |
| --------------------- | ------------------ | ----------------- |
| `modCheckGameVersion` | `CheckGameVersion` | 版本检测 mod 实例 |

## 子路径导出

| 子路径               | 描述   |
| -------------------- | ------ |
| `.`                  | 主入口 |
| `./CheckGameVersion` | 主类   |

## API 参考

#### CheckGameVersion <Badge text="类" />

实现 `Sc2EventTracerCallback`。方法：`checkGameVersion()`、`whenSC2StoryReady()`。

## 类型定义来源

<SourceCode href="https://github.com/Lyoko-Jeremie/Degrees-of-Lewdity_Mod_CheckGameVersion" />

> [!INFO]
>
> - 上游: https://github.com/Lyoko-Jeremie/Degrees-of-Lewdity_Mod_CheckGameVersion
> - 默认分支: master
> - 构建: `pnpm run download && pnpm run build`
