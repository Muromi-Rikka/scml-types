---
title: Modded Clothes Addon
---
import { PackageManagerTabs } from '@rspress/core/theme';

# @scml/addon-modded-clothes

Modded clothes addon 的 TypeScript 类型定义。

## 安装

<PackageManagerTabs command="install -D @scml/addon-modded-clothes" />

## 用法

### 主入口（全局类型）

```ts
/// <reference types="@scml/addon-modded-clothes" />
```

主入口增强以下全局属性：`addonModdedClothesAddon`（`ModdedClothesAddon` 实例）。

### 子路径导入

```ts
import type { SomeType } from '@scml/addon-modded-clothes/ModdedClothesAddon';
```

## 导出

| 子路径 | 描述 |
|--------|------|
| `.` | 主入口，增强 Window/全局 |
| `./ModdedClothesAddon` | 类型声明 |
| `./ModdedClothesAddonParams` | 类型声明 |

## 类型

### 主入口
- **`addonModdedClothesAddon`** – 添加并修补 modded 服装数据的 Addon。

### `./ModdedClothesAddon`
- **`ModdedClothesAddon`** – 管理 `clothesAddInfo: Map<string, ClothesAddInfo>` 的 Addon 插件。方法：`registerMod()`、`addClothes()`、`exportDataZip()`、`init()`。
- **`ClothesAddInfo`** – `{ modName, clothes: ClothesInfo[], patch: ClothesPatchInfo[] }`。
- **`ClothesInfo`** – `{ key, data: ClothesItem[] }`。
- **`searchClothesByName()`**、**`checkForOutfitWarmth()`** – 工具函数。

### `./ModdedClothesAddonParams`
- **`ModdedClothesAddonParams`** – 配置：`clothes: AddClothesItem[]`、`patch?: string[]`。
- **`AddClothesItem`**、**`ClothesPatchInfo`**、**`ClothesPatchUpdateType`** – 参数类型。
- **`checkParams`**、**`checkAddClothesItem`**、**`checkClothesPatchInfo`** – 类型守卫。

## 类型定义来源

- 上游: https://github.com/Lyoko-Jeremie/DoL_ModdedClothesAddon
- 默认分支: master
- 构建: `pnpm run download && pnpm run build`
