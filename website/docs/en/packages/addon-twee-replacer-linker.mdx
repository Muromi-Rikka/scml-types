---
title: Twee Replacer Linker Addon
---

import { Badge, Tab, Tabs, PackageManagerTabs, Steps, SourceCode } from '@rspress/core/theme';

# @scml/addon-twee-replacer-linker

TypeScript type definitions for Twee replacer linker addon, for connecting TweeReplacer clients and coordinating replace callbacks across mods.

## Installation

<PackageManagerTabs command="install -D @scml/addon-twee-replacer-linker" />

## Quick Start

<Steps>

### Add type reference

```ts
/// <reference types="@scml/addon-twee-replacer-linker" />
```

### Use global instance

`addonTweeReplacerLinker` is mounted on `window`.

### Import types on demand

```ts
import type { TweeReplacerLinker } from '@scml/addon-twee-replacer-linker/TweeReplacerLinker';
import type { TweeReplacerLinkerInterface, TweeReplacerLinkerClientInterface } from '@scml/addon-twee-replacer-linker/TweeReplacerLinkerInterface';
```

</Steps>

## Global Types <Badge type="info" text="global" />

| Property | Type | Description |
|----------|------|-------------|
| `addonTweeReplacerLinker` | `TweeReplacerLinker` | Addon instance, coordinates TweeReplacer clients |

<Tabs>
<Tab label="Main entry">

```ts
/// <reference types="@scml/addon-twee-replacer-linker" />
```

</Tab>
<Tab label="Subpath import">

```ts
import type { TweeReplacerLinker } from '@scml/addon-twee-replacer-linker/TweeReplacerLinker';
import type { TweeReplacerLinkerInterface } from '@scml/addon-twee-replacer-linker/TweeReplacerLinkerInterface';
```

</Tab>
</Tabs>

## Subpath Exports

| Subpath | Description |
|---------|-------------|
| `.` | Main entry, augments Window/global |
| `./TweeReplacerLinker` | Main class |
| `./TweeReplacerLinkerInterface` | Interfaces and types |

## API Reference

### ./TweeReplacerLinker

#### TweeReplacerLinker <Badge text="class" />

| Property/Method | Type | Description |
|-----------------|------|-------------|
| `canRegister` | `boolean` | Whether registrable |
| `userCallback` | `TweeReplacerLinkerCallbackData[]` | User callback list |
| `registerClient()` | `(client) => Promise<boolean>` | Register client |
| `addUserMod()` | `(clientName, userModName, callback) => Promise<boolean>` | Add user mod callback |
| `registerMod()` | `(addonName, mod, modZip) => Promise<void>` | Register mod |
| `afterPatchModToGame()` | `() => Promise<void>` | After-patch hook |
| `init()` | `() => void` | Initialize |

### ./TweeReplacerLinkerInterface

#### TweeReplacerLinkerInterface <Badge text="interface" />

| Method | Description |
|--------|-------------|
| `registerClient(client)` | Register client |
| `addUserMod(clientName, userModName, callback)` | Add user mod callback |

#### TweeReplacerLinkerClientInterface <Badge text="interface" />

| Method | Description |
|--------|-------------|
| `enableLinkerMode()` | Enable linker mode |

#### TweeReplacerLinkerClientCallbackType <Badge text="type" />

`(sc: SC2DataInfo) => Promise<any>` - User mod callback type

## Type Definition Source

<SourceCode href="https://github.com/Lyoko-Jeremie/TweeReplacerLinkerAddon" />

> [!INFO]
> - Upstream: https://github.com/Lyoko-Jeremie/TweeReplacerLinkerAddon
> - Default branch: master
> - Build: `pnpm run download && pnpm run build`
