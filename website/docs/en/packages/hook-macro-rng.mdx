---
title: Macro RNG Hook
---

import { Badge, Tab, Tabs, PackageManagerTabs, Steps, SourceCode } from '@rspress/core/theme';

# @scml/hook-macro-rng

TypeScript type definitions for Macro RNG hook, mounting RNG used by SugarCube macros for deterministic or custom RNG behavior.

## Installation

<PackageManagerTabs command="install -D @scml/hook-macro-rng" />

## Quick Start

<Steps>

### Add type reference

```ts
/// <reference types="@scml/hook-macro-rng" />
```

### Use global instance

`hookMacroRng` is mounted on `window`.

</Steps>

## Global Types <Badge type="info" text="global" />

| Property | Type | Description |
|----------|------|-------------|
| `hookMacroRng` | `HookMacroRng` | Macro RNG Hook instance |

## Subpath Exports

| Subpath | Description |
|---------|-------------|
| `.` | Main entry |
| `./HookMacroRng` | Type declarations (entry: inject_early) |

## API Reference

#### HookMacroRng <Badge text="class" />

Addon class that installs macro RNG hook.

## Type Definition Source

<SourceCode href="https://github.com/Lyoko-Jeremie/DoLHookMacroRng" />

:::info
- Upstream: https://github.com/Lyoko-Jeremie/DoLHookMacroRng
- Default branch: master
- Build: `pnpm run download && pnpm run build`
:::
