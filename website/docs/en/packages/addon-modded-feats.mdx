---
title: Modded Feats Addon
---

import { Badge, Tab, Tabs, PackageManagerTabs, Steps, SourceCode } from "@rspress/core/theme";

# @scml/addon-modded-feats

TypeScript type definitions for Modded feats addon, for adding and merging modded feats data.

## Installation

<PackageManagerTabs command="install -D @scml/addon-modded-feats" />

## Quick Start

<Steps>

### Add type reference

```ts
/// <reference types="@scml/addon-modded-feats" />
```

### Use global instance

`addonModdedFeatsAddon` is mounted on `window`.

### Import types on demand

```ts
import type { ModdedFeatsAddon } from "@scml/addon-modded-feats/ModdedFeatsAddon";
import type { ModdedClothesFeatsParams } from "@scml/addon-modded-feats/ModdedFeatsAddonParams";
```

</Steps>

## Global Types <Badge type="info" text="global" />

| Property                | Type               | Description                        |
| ----------------------- | ------------------ | ---------------------------------- |
| `addonModdedFeatsAddon` | `ModdedFeatsAddon` | Addon instance, manages feats data |

<Tabs>
<Tab label="Main entry">

```ts
/// <reference types="@scml/addon-modded-feats" />
```

</Tab>
<Tab label="Subpath import">

```ts
import type { ModdedFeatsAddon } from "@scml/addon-modded-feats/ModdedFeatsAddon";
import type { ModdedClothesFeatsParams } from "@scml/addon-modded-feats/ModdedFeatsAddonParams";
```

</Tab>
</Tabs>

## Subpath Exports

| Subpath                    | Description                        |
| -------------------------- | ---------------------------------- |
| `.`                        | Main entry, augments Window/global |
| `./ModdedFeatsAddon`       | Main class and utility functions   |
| `./ModdedFeatsAddonParams` | Config params and type guards      |

## API Reference

### ./ModdedFeatsAddon

#### ModdedFeatsAddon <Badge text="class" />

| Property          | Type                                        | Description       |
| ----------------- | ------------------------------------------- | ----------------- |
| `featsData`       | `Map<string, FeatsObject>`                  | Feats data by key |
| `registerMod()`   | `(addonName, mod, modZip) => Promise<void>` | Register mod      |
| `exportDataZip()` | `(zip: JSZip) => Promise<JSZip>`            | Export data       |
| `init()`          | `() => void`                                | Initialize        |

#### mergeFeatsObject() / appendFeatsObject() <Badge text="method" />

### ./ModdedFeatsAddonParams

#### ModdedClothesFeatsParams <Badge text="interface" />

| Property | Type       | Description          |
| -------- | ---------- | -------------------- |
| `feats`  | `string[]` | Feats file path list |

#### checkParams() / checkFeatsItem() <Badge text="type guard" />

## Type Definition Source

<SourceCode href="https://github.com/Lyoko-Jeremie/DoL_ModdedFeatsAddon" />

> [!INFO]
>
> - Upstream: https://github.com/Lyoko-Jeremie/DoL_ModdedFeatsAddon
> - Default branch: master
> - Build: `pnpm run download && pnpm run build`
