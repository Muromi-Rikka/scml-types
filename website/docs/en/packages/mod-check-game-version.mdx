---
title: DoL Game Version Check
---

import { Badge, Tab, Tabs, PackageManagerTabs, Steps, SourceCode } from '@rspress/core/theme';

# @scml/mod-check-game-version

TypeScript type definitions for DoL game version check.

## Installation

<PackageManagerTabs command="install -D @scml/mod-check-game-version" />

## Quick Start

<Steps>

### Add type reference

```ts
/// <reference types="@scml/mod-check-game-version" />
```

### Use global instance

`modCheckGameVersion` is mounted on `window`.

</Steps>

## Global Types <Badge type="info" text="global" />

| Property | Type | Description |
|----------|------|-------------|
| `modCheckGameVersion` | `CheckGameVersion` | Version check mod instance |

## Subpath Exports

| Subpath | Description |
|---------|-------------|
| `.` | Main entry |
| `./CheckGameVersion` | Main class |

## API Reference

#### CheckGameVersion <Badge text="class" />

Implements `Sc2EventTracerCallback`. Methods: `checkGameVersion()`, `whenSC2StoryReady()`.

## Type Definition Source

<SourceCode href="https://github.com/Lyoko-Jeremie/Degrees-of-Lewdity_Mod_CheckGameVersion" />

:::info
- Upstream: https://github.com/Lyoko-Jeremie/Degrees-of-Lewdity_Mod_CheckGameVersion
- Default branch: master
- Build: `pnpm run download && pnpm run build`
:::
