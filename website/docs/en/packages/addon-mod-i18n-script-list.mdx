---
title: I18n Script List Addon
---

import { Badge, Tab, Tabs, PackageManagerTabs, Steps, SourceCode } from '@rspress/core/theme';

# @scml/addon-mod-i18n-script-list

TypeScript type definitions for I18n Script List addon, for loading different script file lists by language.

## Installation

<PackageManagerTabs command="install -D @scml/addon-mod-i18n-script-list" />

## Quick Start

<Steps>

### Add type reference

```ts
/// <reference types="@scml/addon-mod-i18n-script-list" />
```

### Use global instance

`modI18nScriptList` is mounted on `window`.

### Import types on demand

```ts
import type { I18nScriptList, I18nScriptListParams } from '@scml/addon-mod-i18n-script-list/I18nScriptList';
```

</Steps>

## Global Types <Badge type="info" text="global" />

| Property | Type | Description |
|----------|------|-------------|
| `modI18nScriptList` | `I18nScriptList` | Addon instance |

<Tabs>
<Tab label="Main entry">

```ts
/// <reference types="@scml/addon-mod-i18n-script-list" />
```

</Tab>
<Tab label="Subpath import">

```ts
import type { I18nScriptList } from '@scml/addon-mod-i18n-script-list/I18nScriptList';
```

</Tab>
</Tabs>

## Subpath Exports

| Subpath | Description |
|---------|-------------|
| `.` | Main entry, augments Window/global |
| `./I18nScriptList` | Main class, interfaces, utilities |

## API Reference

### ./I18nScriptList

#### I18nScriptList <Badge text="class" />

| Property | Type | Description |
|----------|------|-------------|
| `info` | `ModRecord[]` | Mod record list |
| `registerMod()` | `(addonName, mod, modZip) => Promise<void>` | Register mod |
| `beforePatchModToGame()` | `() => Promise<void>` | Before-patch hook |
| `init()` | `() => void` | Initialize |

#### I18nScriptListParams <Badge text="interface" />

| Property | Type | Description |
|----------|------|-------------|
| `mainLanguage` | `string` | Main language |
| `languageFile` | `LanguageFileItem[]` | Language to script file mapping |

#### LanguageFileItem <Badge text="interface" />

| Property | Type | Description |
|----------|------|-------------|
| `language` | `string` | Language code |
| `scriptFileList` | `string[]` | Script file path list |

## Type Definition Source

> [!INFO]
> - Upstream: Bundled with scml-types
> - Build: `pnpm run download && pnpm run build`
