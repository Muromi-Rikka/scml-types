---
title: CoT Game Version Check
---

import { Badge, Tab, Tabs, PackageManagerTabs, Steps, SourceCode } from "@rspress/core/theme";

# @scml/mod-cot-check-game-version

TypeScript type definitions for CoT (Cape on the Tower) game version check.

## Installation

<PackageManagerTabs command="install -D @scml/mod-cot-check-game-version" />

## Quick Start

<Steps>

### Add type reference

```ts
/// <reference types="@scml/mod-cot-check-game-version" />
```

### Use global instance

`modCheckGameVersion` is mounted on `window`.

</Steps>

## Global Types <Badge type="info" text="global" />

| Property              | Type               | Description                    |
| --------------------- | ------------------ | ------------------------------ |
| `modCheckGameVersion` | `CheckGameVersion` | CoT version check mod instance |

## Subpath Exports

| Subpath              | Description |
| -------------------- | ----------- |
| `.`                  | Main entry  |
| `./CheckGameVersion` | Main class  |

## API Reference

#### CheckGameVersion <Badge text="class" />

Addon class that validates CoT game version at runtime.

## Type Definition Source

<SourceCode href="https://github.com/Lyoko-Jeremie/CheckGameVersionCot" />

> [!INFO]
>
> - Upstream: https://github.com/Lyoko-Jeremie/CheckGameVersionCot
> - Default branch: master
> - Build: `pnpm run download && pnpm run build`
