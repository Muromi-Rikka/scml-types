{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["download", "^build"],
      "inputs": ["remote/**"],
      "outputs": ["type-dist/**"]
    },
    "download": {
      "outputs": ["remote/**"]
    }
  }
}
